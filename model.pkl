import joblib

model = LogisticRegression(
    penalty='elasticnet',
    solver='saga',
    l1_ratio=0.5,
    C=10,
    max_iter=1000
)

# Train the model
model.fit(X_train, y_train)

joblib.dump(model, "model.pkl")